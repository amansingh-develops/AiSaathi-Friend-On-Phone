<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Solid fill as base (ensures visibility) -->
    <solid android:color="#00A0E0" />
    <!-- Gradient overlay -->
    <gradient
        android:type="radial"
        android:startColor="#00C6FF"
        android:endColor="#0072FF"
        android:gradientRadius="50%p"
        android:centerX="0.5"
        android:centerY="0.5" />
</shape>
